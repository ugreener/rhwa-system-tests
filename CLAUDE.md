# CLAUDE.md

Project guidance lives in [AGENTS.md](AGENTS.md). Do not add content here or create new sections — update AGENTS.md instead.

The only exception is Claude Code-specific configuration (hooks, slash commands, permission overrides) with no equivalent in AGENTS.md.
